# Gesetze werden nicht mehr aus YAML geladen.
#
# Single Source of Truth: PostgreSQL-Tabelle `gesetze` + `gesetze_i18n`,
# gepflegt über Alembic-Migrationen unter backend/app/db/migrations/versions/.
#
# API: GET /api/content/gesetze?locale=de|en
# Legacy-Bundle: GET /api/content/bundle?locale=de|en&scenario_id=standard

[]