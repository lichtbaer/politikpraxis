# Nur statische SPA — Routing /api erfolgt im Edge-nginx (docker-compose.prod.yml)
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
